import { t } from "ttag";

import { SettingsPageWrapper } from "metabase/admin/components/SettingsSection";

export const ToolsUpsell = () => {
  return <SettingsPageWrapper title={t`Erroring questions`} />;
};
